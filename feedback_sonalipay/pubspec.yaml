name: feedback_sonalipay
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  file_picker: ^8.0.6
  # firebase_core: ^3.2.0
  # cloud_firestore: ^5.1.0
  # firebase_storage: ^12.1.1
  # firebase_database: ^11.0.3
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  # flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
